{
  "name": "thebadge-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ developerfred/thebadge-test",
    "create-local": "graph create --node http://localhost:8020/ developerfred/thebadge-test",
    "remove-local": "graph remove --node http://localhost:8020/ developerfred/thebadge-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 developerfred/thebadge-test",
    "test": "graph test",
    "auth:codingsh": "graph auth --product hosted-service a3195ebe9408404483f3bca0c0ec6424",
    "deploy:matic-codingsh": "yarn build:matic && graph deploy --product hosted-service developerfred/thebadge-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.62.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
